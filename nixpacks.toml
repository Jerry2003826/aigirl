[phases.setup]
# Railway/Nixpacks 默认会调用 npm ci；这里改为 npm install，并保留 optional 依赖（如 rollup 平台二进制）
install = [
  "npm install --ignore-scripts=false"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npm run start"

