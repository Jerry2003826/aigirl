# 1. 设置 Node 版本（可选，但推荐）
[phases.setup]
nixPkgs = ["nodejs"]

# 2. 核心修改：这里必须是 [phases.install]，且使用 cmds
[phases.install]
cmds = ["npm install"]

# 3. 构建命令
[phases.build]
cmds = ["npm run build"]

# 4. 启动命令
[start]
cmd = "npm run start"