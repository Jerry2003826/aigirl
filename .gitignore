node_modules
dist
dist-web
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Config files (may contain secrets)
config/app.config.json
config/app.config.local.json
config/env.local

# Runtime outputs / local artifacts (do not commit)
logs/
public/uploads/
attached_assets/

# IDE / local workspace
.idea/
.vscode/
.config/
.local/

# Reverse proxy local configs
nginx/*.conf
!nginx/example.conf
caddy/Caddyfile
!caddy/Caddyfile.example

# Local runtime config / secrets (do not commit)
.env
.env.*
config/app.config.json
config/app.config.local.json

# Local env file (no dotfile, easier on Windows)
config/env.local